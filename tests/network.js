const net = require('net');


const endpoint = (port) => (
	`http://127.0.0.1:${port}`
)

describe('Connection', function () {

	// describe('manage broken request', function () {
	// 	it('does not block on everlasting request', function (done) {
	// 		this.timeout(60000);
	// 		done();
	// 	});
	// });

});